# 字灵（Zi Ling）— 产品设计书（意图与原则）

> **文档角色**：产品宪法、交互与视觉原则、与将来系统对接的**语义契约**（非实现细节）。  
> **执行路线**：见 `PLAN.md`。

---

## 1. 辨形宪法（北极星）

1. **辨形优先于炫技**：动态过程中，用户应能稳定识别「当前轮廓 / 字形」；若动效削弱识别，**削减动效**而非削轮廓。  
2. **单一形信息通道**：轮廓可读性依赖 **低频边界与笔画连通性**；体内高频运动易将字形退化为「纹理」，须用 **分层与预算** 约束。  
3. **参数分层（心智模型）**：  
   - **形变**：改变可走区域、边界、宏观轮廓（将来与 **AI 矩阵** 强相关）。  
   - **纹理动效**：在形不变或微变的前提下，调节体内位移、交换、波纹等。  
   侧栏与文档应逐步让用户感知这一分层（可先文档、后 UI）。

---

## 2. 形场（Shape Field）— 核心抽象

### 2.1 定义（概念）

- **形场**：与字灵 **逻辑栅格** 对齐的离散场，至少包含 **可走 / 不可走**（是否与「字笔画」同构视形态而定）。  
- **权威来源**：渲染、粒子是否「越界」、淡出/补位等 **必须** 与同一套形场一致，避免逻辑与画面分叉。

### 2.2 壳 / 芯 / 边界（软分层，可逐步实现）

| 概念 | 意图 |
|------|------|
| **壳（shell）** | 贴近轮廓边界的格；宜 **低动量或切向运动**，锁住「形」的第一印象。 |
| **芯（core）** | 内部区域；可承担 **交换、蛇行、谐波** 等较强动效（仍受形场约束）。 |
| **边界（boundary）** | 形场与外部的交界；**开放边界条件**（淡出、补入、重生）主要发生在此带。 |

不强制首版就实现独立 shell 掩码；但 **新功能评审** 须回答：该动效主要影响壳、芯还是边界？

---

## 3. 外部矩阵与 API（预留契约 · 暂不实现）

> **伏笔**：将来由 AI 经 API 下发 **矩阵流**，驱动形场随时间向目标演变。本节只约定 **原则与对接面**，不定 JSON 字段、不写客户端。

### 3.1 Producer / Consumer

- **Producer（外部）**：生成「目标形场」或「形变增量」矩阵（可能含置信度等通道）。  
- **Consumer（字灵）**：读取 **当前形场** + **目标/增量**，在 **形变预算** 内更新下一帧形场，再驱动粒子与渲染。

### 3.2 对齐原则

- 字灵侧保留 **固定逻辑格距与原点规则**；外部矩阵若分辨率不同，经 **重采样与对齐层** 再进入 Consumer，**不把对齐散落**在粒子逻辑各处。  
- **时间维**：矩阵更新可快可慢；Consumer 必须 **限速、滤波**，避免轮廓闪烁（与 `PLAN.md` 风险表一致）。

### 3.3 与「叙事层」的关系

- **轮廓外淡出、由外缘补入** 与「场从边界更新」意象一致，适合作为将来 **矩阵–粒子** 协同的默认叙事，而非临时特效。

---

## 4. 运动范式与辨形（纲要）

每种范式应明确 **不变量**（用户可见差异的根源），例如：

- **谐波（严格格点）**：形场内离散格目标；连续亚格位移仅在为「纹理」显式开启时出现。  
- **蛇行**：粒子约束在 **走廊子图** 上，禁止「横穿」捷径。  
- **邻接交换（规划）**：仅允许与邻居 **合法换位**，禁止叠字与交错穿透。  
- **轮廓游走**：探索性强，**叠字风险升高**，须由分离算法与壳层约束兜底。

「轨」的区分度应来自 **不变量差异**，而非仅速度或振幅系数。

---

## 5. 实现挂钩（与代码仓库）

| 构件 | 路径 / API | 说明 |
|------|------------|------|
| 形场算法模块 | `js/ziling/shape-field.js` → `window.ZiLingShapeField` | `buildWalkSetFromLocalPoints`、`shellCellsFromWalkSet`、`bandAtGrid` |
| Pet 消费 | `pet.js`：`Pet._rebuildShapeFieldFromForm`、`dumpShapeField`、`_textureBudgetMul` | 换形后重建；蛇形态跳过；`window.ZiLing.ShapeField` 引用模块 |
| 调试 | `?shapeDebug=1`，控制台 `_shapeDump()` | 摘要非全矩阵，避免巨量 JSON |

将来 **AI Consumer** 可向 `Pet` 注入与 `walk` 同构的集合，或替换为 `Uint8Array` 密铺 + 与 `gridCell` 对齐层。

---

## 6. 与 `PLAN.md` 的关系

- **DESIGN.md**：「为什么」与「什么不可违背」。  
- **PLAN.md**：「分阶段做什么」与「如何验收」。  
- 实现细节、版本号级 changelog 可留在代码旁注或独立 `CHANGELOG`；本设计书 **不追逐每一行 diff**。

---

## 7. 变更记录（设计书摘要）

| 版本 | 摘要 |
|------|------|
| 0.1 | 首版：辨形宪法、形场、壳/芯/边界、外部矩阵预留、运动范式纲要、与 PLAN 分工 |
| 0.2 | §5 实现挂钩：`shape-field.js`、`dumpShapeField`、`shapeDebug` URL |
