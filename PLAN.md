# 字灵（Zi Ling）— 工程与产品计划书

> **文档角色**：可执行的路线与验收口径；与 `DESIGN.md`（意图与原则）配套。  
> **当前构建**：以仓库 `index.html` 中 `ziling-build` / 页头 `buildStamp` 为准（若存在）。

---

## 0. 北极星与长期伏笔

### 0.1 北极星：辨形优先

- **辨形**：用户在动态过程中仍能稳定读出「当前是什么字 / 什么轮廓」——优先于炫技动效、粒子密度或模式数量。  
- 一切侧栏参数、运动轨、淡出/补位逻辑，**默认不得破坏**可辨识的形信息；若冲突，**压体内动效、保形场**。

### 0.2 长期伏笔：外部 AI 矩阵驱动实时形变（暂不实现）

- 未来期望：**通过 API 读取 AI 输出的离散场（矩阵）**，驱动字灵在时间上向目标轮廓演变。  
- **本阶段不开发** AI 接入、网络层、具体矩阵格式；但在架构与文档上预留 **「形场」单一真源** 与 **producer 可替换**，避免日后推倒重来。

### 0.3 两文档分工

| 文档 | 内容 |
|------|------|
| `DESIGN.md` | 辨形宪法、形场语义、分层（壳/芯/边界）、外部矩阵预留契约（原则级） |
| `PLAN.md`（本文） | 阶段划分、里程碑、工程任务、风险、与现状功能的映射 |

---

## 1. 概念框架：形 / 动 / 叙事

将运行时拆成三层，便于与「矩阵流」对齐：

| 层 | 含义 | 将来与矩阵的关系 |
|----|------|-------------------|
| **形** | 可走格集合、边界、（可选）壳层掩码；渲染与约束的权威来源 | AI **producer** 输出目标形场；本地 **consumer** 做插值与限速 |
| **动** | 在形约束下的位移、交换、蛇行、谐波等 | 形变剧烈时 **自动压低** 体内纹理动效（形变预算） |
| **叙事** | 外逸淡出、边界补入、重生等开放边界语义 | 与「场从边界注入」意象一致，易与矩阵更新节拍对齐 |

**原则**：动与叙事都 **只读** 当前帧形场（及短窗内的形变目标），不另起一套「和栅格不一致」的形。

---

## 2. 阶段路线（策划版）

### 2.1 近期（巩固辨形与可接矩阵的「坑位」）

| ID | 内容 | 验收口径（示例） | 状态 |
|----|------|------------------|------|
| P1 | **形场单一真源**：mask / 可走格与绘制、生命周期判定同源 | 无「逻辑在形内、绘制像形外」的长期漂移 | **已落地（本仓库）**：`js/ziling/shape-field.js` + `Pet._rebuildShapeFieldFromForm` 由全体 `tx,ty` 生成 walk/shell；与弹簧目标同源 |
| P2 | **壳/芯/边界** 概念写入设计与代码注释（可先软分层） | 复杂巨字：壳层笔画连通性感知不被体内平移长期打断 | **已落地（软分层）**：`g.shapeBand` shell/core；壳层压低 `bw`/rumble |
| P3 | **运动模式可解释**：每种轨一句话说清「保哪层形、禁止什么」 | 用户/文档能区分轨；非换系数糊弄 | **待办**（本精简版无多轨 UI；见 DESIGN §4） |
| P4 | **形变 vs 纹理参数** 概念分离（可先文档级） | 侧栏心智：将来「矩阵过渡」占形变通道，速/徙多占纹理通道 | **部分**：换形触发 `_shapeMutationT` 纹理预算；文档见 DESIGN |

### 2.2 中期（工程接口，仍可不接 AI）

| ID | 内容 | 说明 | 状态 |
|----|------|------|------|
| M1 | **形场接口形状收敛**：如每帧 `walkable` 栅格 + 可选 `shell` / `confidence` | 体内调度、淡出判定只依赖该接口 | **部分**：walk + shell `Set`；无 Uint8Array 密铺（后续可加密栅格） |
| M2 | **快照与调试钩子**：开发模式下可 dump 当前形场一格一态 | 便于将来对矩阵 diff / 回放 | **已落地**：`?shapeDebug=1` → `window._shapeDump()`、`pet.dumpShapeField()` |
| M3 | **形变预算**：全局「每帧允许形边界变化量」上限 | 矩阵抖动时与体内运动抢带宽的问题可控 | **占位**：换形后 0.72s 内 `_textureBudgetMul()` 压低纹理 |
| M4 | **输入矩阵预处理（占位）**：低通、置信度门限、与 `gridCell` 对齐策略 | 文档与空模块即可，实现可后移 | **待办** |

### 2.3 远期（AI / API）

| ID | 内容 |
|----|------|
| F1 | 矩阵协议定稿（分辨率、对齐、通道含义、时间戳） |
| F2 | Producer：拉流 / WebSocket / 轮询 + 背压 |
| F3 | 形变插值：当前形场 → 目标矩阵（速度约束 + 与粒子调度协同） |
| F4 | 失败降级：无 API 时回退本地形源（宏字 / 用户字） |

---

## 3. 与已讨论产品手段的映射（不重复实现细节）

以下在 `DESIGN.md` §2.x 中展开意图；此处只标 **在辨形+矩阵伏笔下的定位**：

- **轮廓外淡出 + 边界补入**：叙事层，**开放边界**，与将来矩阵从外缘影响场一致；需控节奏避免「边界沸腾」。  
- **邻接交换 / 规整路径换位**：动效层的一种 **强区分** 范式，减少叠字与交错；长期可与矩阵形变 **分时**（形变大时降频交换）。  
- **全画面字场 + 巨字区域激活**：舞台层增强，**性能与状态规模大**；建议排在形场接口稳定之后，或先做低分辨率「纹理场」轻量版。  
- **严格格点谐波 + 可选亚格颤抖**：当前辨形向的折中；在矩阵时代归入 **纹理动效**，服从形变预算。

---

## 4. 风险登记

| 风险 | 缓解（策划） |
|------|----------------|
| AI 矩阵帧间抖动 | 时间低通、置信度门限、形变限速 |
| 矩阵更新快 + 体内动效强 | 形变–纹理联动：形变 `>` 阈值则压体内振幅 |
| 矩阵分辨率 ≠ 字灵 grid | 固定单一逻辑格距；独立 **对齐/重采样层** |
| 运动模式「看起来都一样」 | 每种模式 **不同的不变量**（如交换禁叠字、蛇行禁离走廊）并写进 UI 文案 |
| 复杂巨字一动就糊 | 壳层稳定 + 时间多路复用（长稳短动）+ 对比度预算 |

---

## 5. 版本与变更记录（计划书自身）

| 版本 | 说明 |
|------|------|
| 0.2 | 工程：形场模块 `js/ziling/shape-field.js`；Pet 壳/芯阻尼与换形纹理预算；`shapeDebug`；`nuisTap` 修复 |
| 0.1 | 首版：辨形北极星、AI 矩阵伏笔、形/动/叙事三层、近中远期、风险表、与 DESIGN 分工 |

---

## 6. 与 README / 代码仓库

- 若主仓库含历史章节号（如 `1.35` 剪影格点等），**不删除**；本计划书为 **上层总纲**，可在具体版本小节末加一行「参见 `PLAN.md` §2」交叉引用。  
- 新贡献者：**先读 `DESIGN.md` 辨形与形场原则，再读本文阶段表**。
