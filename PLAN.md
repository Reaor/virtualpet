# 字灵（Zì Líng）产品计划书

> 对照需求逐项落地；完成项打勾，未做或部分完成写明阻塞与下一步。

## 0. 如何确认你看到的是「本计划对应的构建」

| 检查项 | 预期 |
|--------|------|
| 页头副标题 | 含 **栅格字灵 · 日程与活字** |
| 页头小字 | **build 2.1.0**（随 `index.html` 中 `ziling-build` 递增） |
| 画布外舞台 | 深色渐变，**非**暖黄宣纸大底 |
| 页面上方 | 有 **躯体字** 输入 + **AI 建议** 多行区 |
| 诗笺区 | 有 **日程试字** 标签（未完成 / 已完成 / 拖延 / 待办） |

若以上不符：多为 **GitHub Pages 缓存** 或 **发布分支不是 `gh-pages` / 未合并 `main`**。请无痕窗口或 `?v=日期` 强刷；仓库 **Settings → Pages** 核对发布源。

---

## 1. 需求对照（你的复述 → 实现状态）

### 1.1 形态更多、更灵动（觅食巡游）

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 形态数量 | **部分完成** | 已有 cloud / crane / star 等；可再加日程隐喻剪影 |
| 蛇形观感 | **已移除** | 已删除 `snake` 专态；觅食保持当前形态，字为**世界格逐格闪现** |
| 多戳难以忍受变形 | **已完成** | `nuisTap` 烦躁换形（曾修 `alt` bug） |

### 1.2 眼睛/表情不突兀；字层表情 + 必要时全字冲击

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 眉眼用字粒子 | **已完成** | `faceRole` + `EXPRESSIONS` 字符眉眼 |
| 情绪不全屏换「悲喜」 | **已完成** | `_hintEdgeChars` 仅外圈；`flashUnifiedChar` 用于重要冲击 |
| 开阔表现 | **待办** | 可增加「眉心一字」「口型区」等更多字层槽位 |

### 1.3 躯体不散、有层次与艺术感

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 隐形格 | **已完成** | 目标吸附 `gridCell` |
| 活字栅 | **已完成** | `gridUnity`：em 阶梯字号、4° 离散倾角、绘制像素对齐、换形落格弹簧 |
| 更「整」的轮廓 | **待办** | 可对 `sampleSilhouette` 步长加密、或形态内二次聚类排字 |

### 1.4 输入口：日程字进躯体 + AI 规整输出再融入

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 躯体单行输入 | **已完成** | `attachBodyChars` + UI |
| AI 建议块 | **已完成** | `ingestAiSuggestionBlock`（躯体:/冲击: 行协议） |
| 真接大模型 API | **未做** | 需密钥与后端或用户自带 API；前端只保留协议与占位 |

### 1.5 吞噬字 + 伪 UI 背景；不要宣纸

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 夜雾 + 栅格伪 UI | **已完成** | `styles.css` + canvas 底 |
| 类名「paper」误导 | **已改名** | `paper-grain` → `stage-scanlines`（语义对齐） |

### 1.6 形态由吞噬字 + 输入共同塑造；不要「骨、肉」填形

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 不用骨肉体字面 | **已核对** | 默认池与情绪池无此类字；**禁止**往 `MOOD_POOLS` 塞解剖隐喻字 |
| `formDigestBias` | **已完成** | 鱼/月/花等字提高对应形权重 |
| 字序随吞噬渐变 | **待办** | 可按 `eatenChars` 比例逐步替换非眉眼粒子 |

### 1.7 日程词：未完成 / 完成 / 拖延

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 规则 + 单字 hint | **已完成** | `digestText` / `DIGEST_RULES` |
| 多词计分 | **已完成** | 避免「已完成」里单字误判 |

### 1.8 栅格字：平滑落格 + 轻量液体感（性能优先）

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 局部一格一字 | **已完成** | `_resolveUniqueLocalGrid` |
| 目标吸附格心 + **平滑**弹簧 | **已完成** | 关闭逐格闪现；`gridSnapping` 下目标 round 到格心 |
| 波面起伏 | **已完成** | `fluidStrength` + `_fluidPhase` 正弦叠加在目标位 |
| 邻域凝聚 | **已完成** | 空间哈希桶质心弱吸引（非全对全 SPH） |
| 卡顿优化 | **已完成** | 去掉体积光晕二次绘制；粒子 150；弹簧略软 |
| ~~世界格逐格闪现~~ | **已弃用** | 伤眼、与液体感冲突 |

---

## 2. 近期迭代顺序（建议）

1. **发布与验证**：合并 `main` ↔ `gh-pages` 策略写进 README；CI 可选自动同步 `gh-pages`。  
2. **形态**：+2～3 个「日程隐喻」剪影（钟、简、针）。  
3. **觅食路径**：多段贝塞尔 / 噪声曲线（已去除蛇形专态；字为**世界格逐格闪现**）。  
4. **字池渐变**：吞噬越多，`replace` 越偏向 `eatenChars`。  
5. **AI**：可选 `fetch` 到用户配置 endpoint（无密钥则仅演示）。

---

## 3. 版本号

- 见 `index.html`：`<meta name="ziling-build" content="…" />`  
- 每次对外可见发布前 **递增** `content`，与页头 `buildStamp` 一致。
