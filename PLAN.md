# 字灵（Zì Líng）产品计划书

> 对照需求逐项落地；完成项打勾，未做或部分完成写明阻塞与下一步。  
> **当前构建**：见 `index.html` 中 `ziling-build`（与页头 `buildStamp` 一致；近期为 **2.9.0** 起）。

## 0. 如何确认你看到的是「本计划对应的构建」

| 检查项 | 预期 |
|--------|------|
| 页头小字 | **build ×.×.×** 与 `meta ziling-build` 一致 |
| 脚本 URL | `./pet.js?v=…` / `./app.js?v=…`（强刷缓存） |
| 页面 | 浅色 App 风；**无**整段「AI 建议」面板（协议仍可在代码 `ingestAiSuggestionBlock` 使用） |

若不符：多为 **CDN/浏览器缓存**。请无痕窗口或核对 Network 里 `pet.js` 响应内容首行附近版本。

---

## 1. 需求对照（含你最近一次大反馈）

### 1.9 表情用「字」拼轮廓 · 大字倒计时 · 全身动物剪影 · 去月 · 龙可见 · 换形不挤左上角 · 运动更紧凑

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 表情：汉字沿剪影，不是 emoji 堆一团 | **已完成（2.8.0）** | `emoji_face_a/b/c` 使用 `charPalette` + 同一套剪影采样；`_applyEmojiPaletteIfNeeded` 改为读 `charPalette` |
| 字组成的倒计时（清晰可见） | **已完成（2.8.0）** | `clock` 形态点阵 **`cell` 加大**（约 `S*0.042`）；`?form=clock` 直接看；idle 下每分钟刷新 |
| 猫 / 狐 / 兔 / 鹤：要全身轮廓不要「只有一个头」 | **已完成（2.8.0）** | 四类形态改为 **全身剪影**（躯干、肢、尾/翅/耳等） |
| 月形态去掉 | **已完成（2.8.0）** | 删除 `moon`；`FORM_ORDER` 与 `CHAR_FORM_BIAS` 中 **月 → 星** |
| 容易切到龙 / 龙形态可见 | **已完成（2.8.0）** | **`dragon` 提前到 `FORM_ORDER` 较前**；仍为西方双翼飞龙剪影 |
| 变形跑左上角 | **已完成（2.8.0）** | **根因**：渐进换形目标曾用**局部格**与 **世界格 `mgx/mgy`** 混比。现 `_computeMorphGridTargets` 输出 **`twx/twy` 世界格心**，步进与完成判定一致；`_finishMorph` 反算局部 `tx/ty` 并同步 `mgx/mgy` |
| 运动美感、凑成形体的紧凑感 | **部分完成** | 已 **降低** `fluidStrength` 默认、**减小**巡逻 `pAmp`，减轻「散」；**未做**：轮廓内二次聚类、速度场、B-spline 格路径等 |

### 1.10 四肢/翼/尾的局部动态 · 墨色层次随时间一致变化

| 子需求 | 状态 | 说明 |
|--------|------|------|
| 猫/龙等不只是整体平移，肢体有相对摆动 | **部分完成（2.9.0）** | `classifyAnatom` 按局部坐标分区；`_articulateOffset` 在 **猫/狐/兔/龙/鹤** 上给前肢、后肢、尾、翼、腿、耳、头等 **局部位移 + 微倾角**；与巡逻叠加时在上述形态上 **减半** `internalMotion` 振幅，避免与关节动效打架 |
| 浓淡像绘图层次、且随动态呼吸 | **部分完成（2.9.0）** | `_inkShade`：**顶光（上亮下暗）+ 轮廓 edge + 慢正弦呼吸** 驱动 `alpha` 与字重；渲染用 **混合 shade** 替代纯径向 edge 杂色；眉眼/朱砂层 `skipInk` 保持表情可读 |

### 1.1～1.8 历史条目（摘要）

- **栅格曼哈顿走位、一格一字、渐进换形、躯体字队列、日程词反馈、浅色 UI** 等：已在前序版本落地；细节仍以代码为准。  
- **蛇形**：已移除。  
- **觅食**：沿路径巡游 + 面板吸字；与拖拽解耦防卡死。  
- **AI 面板 UI**：已按产品决策移除；`ingestAiSuggestionBlock` 等 **API 仍保留** 供日后接后端。

---

## 2. 待办与建议顺序（下一波）

| 优先级 | 项 | 说明 |
|--------|----|------|
| P1 | **轮廓内更紧** | 对剪影采样点做 **K 均值 / 条带排序**，让字序沿轮廓或填充更「密实」 |
| P1 | **觅食/睡眠「像动物」** | 觅食锚点路径用 **低曲率折线**；睡眠时整体缩放到卧姿剪影或降低 `patrolAmp` |
| P1 | **关节动效再逼真** | 2.9.0 为 **正弦驱动分区位移**；可升级为 **骨骼链 + IK** 或 **形态专属相位**（左右肢反相等） |
| P2 | **格路径平滑** | 每段格移动 **ease**（短插值）或 **最少弯折** 分配，减少锯齿感 |
| P2 | **计时形态秒级可选** | 当前为 **分**刷新；可加 `clock_sec` 或 URL 参数控制刷新与显示 |
| P3 | **真 AI** | 需 endpoint / 密钥；前端只接协议 |

---

## 2.1 路线图（按你最新方向）：轮廓内游走 · 几何备选 · 幅度与差异化 · 拖拽分流

> **目标一句话**：每个字在**合法区域内**像有简单「寻路/巡线」一样运动，幅度明显、彼此不同；拖拽时整体仍跟手，但**内部字按不同延迟与路径**收敛，避免「整块平移」感。  
> **实现约束**：继续以 Canvas 2D + 现有栅格/弹簧为主；不先上 WebGL，除非性能不够。

### A. 参考方向（上网可搜的关键词，不必逐条抄论文）

| 思路 | 用途 |
|------|------|
| **Cage / 轮廓变形**（cage-based deformation, harmonic coordinates） | 思路借鉴：整体形变由轮廓驱动；我们可简化为 **局部目标点 = 轮廓内合法格上的巡点** |
| **Curve flow / active contour** | 动态轮廓的连续形变；可简化为 **参数曲线 + 法向偏移** 生成「随时间变的合法区域」 |
| **Flow field / 导航网格** | 在**剪影二值 mask** 上建 **可走格图**，用 **A\*** 或 **多源 BFS 距离场** 做「朝目标格走」 |
| **Boids + 障碍**（带边界斥力） | 在轮廓内做 **斥力边界 + 弱对齐**，得到「有序中带乱」的群体轨迹 |
| **Voronoi / Lloyd** | 让字在轮廓内 **均匀分摊空间**，再在各胞内小范围游走，减少重叠与死区 |

这些用于定方向；落地时优先选 **mask + 可走格 + 周期性重选目标格（A\*）** 或 **距离场梯度跟随**，与现有 `gridMarch` 最贴合。

### B. 双轨产品策略（动物难做时的退路）

| 轨道 | 内容 | 何时主推 |
|------|------|----------|
| **B1 · 动物剪影** | 现有 `sampleSilhouette` + **轮廓内合法格** + 分区（肢/翼）不同 **巡逻半径与频率** | 继续迭代猫/龙等 |
| **B2 · 动态几何** | 新增/强化形态：**李萨如、玫瑰线、内旋轮线、分段贝塞尔带、周期变形的星/花**；合法区域 = 曲线扫过的 **粗笔画 mask** 或 **多圆并集** | 当动物动态仍不满意时，用 B2 保证「复杂、漂亮、易控」 |

几何轨更容易做到 **轮廓随时间变、字始终在变形的 mask 内**，验证「子约束在轮廓里」这条假设。

### C. 轮廓内约束（你猜想的「子约束在轮廓里」——技术拆解）

1. **构建 mask（每形态、每帧或低频更新）**  
   - 与现剪影一致：离屏 canvas 画形态 → `getImageData` → **alpha > 阈** 为可走。  
   - 动物：mask 可 **每帧**随 `_articulateOffset` 略变，或 **降采样 + 低频更新** 省算力。

2. **可走格图**  
   - 以 `gridCell` 为步长，中心对齐宠物 `pos`；仅当格心落在 mask 内则 **walkable**。  
   - 眉眼粒子可走 **眼窝附近子集** 或仍走全局 mask（二选一，避免乱入身体外）。

3. **内部「寻路式」游走（替代纯正弦微抖）**  
   - 每字维护 `wanderGoalGx/Gy`（格坐标），周期性或在到达后更换。  
   - 新目标：**在 mask 内随机/泊松采样**，或沿 **边界向内若干格** 的「环带」上选点，增强 **沿边游走** 感。  
   - 移动：`gridMarch` 当前追 **世界投影目标**；改为追 **`baseTx,baseTy` 对应的静态形心目标 + `wander` 偏移**，且 **下一步若越界则沿 mask 边界滑移或重选目标**（碰壁回弹 / 最短步回可走格）。

4. **幅度与差异化**  
   - 提高 **单字最大偏离**（相对 `baseTx,ty`）到 **0.15～0.25 × size** 量级（可调），按 `anatomRole`、`depth`、`patrolSeed` 分层：**频率、相位、步长上限** 三者错开。  
   - 「和谐」：全局加一个 **慢包络**（例如全体共用的低频 `envelope`），避免各字各跳各的拍子。

### D. 拖拽：有序中有乱，不要整块平移

**现状**：`dragTo` 只改 `this.pos.x/y`，字的目标 `tx,ty` 不变 → 世界坐标整体平移，内部相对静止。

**目标行为**：

1. **虚拟锚点分流**：拖拽时除 `pos` 外，为每字维护 **`dragLatch` 或 `lagPos`**，其追 `pos` 的弹簧 **刚度/阻尼按字索引、深度、解剖区哈希不同** → 到达同一最终位置的时间与路径不同。  
2. **格上表现**：在 `gridMarch` 分支里，目标世界格 = `f(base局部, pos, lag, wander)`；lag 大者 **多走几步曼哈顿弯折**，形成「拖尾」秩序。  
3. **速度继承**：`pointer` 移动时把 **切向分量** 乘以小系数加进 `wander` 或 `marchPref` 偏好轴，产生 **被甩动** 感；松手后衰减。  
4. **防脸崩**：`faceRole` 字 **滞后更小** 或 **限制在眼窝 mask**，避免表情散架。

### E. 建议实现顺序（写入代码时的任务切分）

1. ⬜ **Mask + walkable**：从现有 `sampleSilhouette` 抽 **可复用的 mask 构建**（缓存到换形/resize）。  
2. ⬜ **Wander 目标 + 越界处理**：在可走格上选点 + `gridMarch` 追点；越界则重选或沿边滑动。  
3. ⬜ **调参**：`internalMotion` / wander **幅度档位** + 按 `anatomRole` 分层。  
4. ⬜ **拖拽 lag 场**：每字独立一阶/二阶追 `pos` 的偏移，接入 `gridMarch` 目标计算。  
5. ⬜ **B2 原型形态 1～2 个**：例如「李萨如填充环」或「变形星芒带」，专门展示 **大幅度、轮廓内、好看**。  
6. ⬜ （可选）**动物 mask 随关节略变**：与 `_articulateOffset` 同步平移局部采样，否则四肢动态与可走区不一致。

### F. 风险与取舍

| 风险 | 应对 |
|------|------|
| 每帧全画布 mask 太贵 | mask **缩放采样**（与现 280 cap 一致）、**仅动物形态**更新或 **每 2～3 帧** 更新 |
| 字挤出剪影 | 严格 **格心 in mask**；失败则回退到上一合法格 |
| 与渐进换形冲突 | morph 期间 **冻结 wander 重选** 或 **仅允许沿 morph 目标移动**（与现 `_articulateOffset` 在 morph 关闭一致） |

---

## 3. 版本号

- 见 `index.html`：`<meta name="ziling-build" content="…" />`  
- 每次对外可见发布前 **递增** `content`，并同步更新 **`pet.js` / `app.js` 的 `?v=`** 以便强刷缓存。
